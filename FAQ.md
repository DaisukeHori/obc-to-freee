# FAQ — obc_to_freee.py よくある質問

## 入力ファイル形式まわり

### Q-A1. 奉行 CSV が UTF-8 形式でも読めますか?

はい。**CP932 (Shift-JIS) / UTF-8 / UTF-8 BOM / UTF-16** をすべて自動判定して読み込みます。何も指定しなくても自動でエンコーディングを認識します。明示指定したい場合は `--encoding cp932` のように指定可能です (`auto` / `cp932` / `utf-8` / `utf-8-sig` から選択)。

※ UTF-16 は自動判定のみ対応。`--encoding utf-16` のような明示指定は不可です (`--encoding` の選択肢は auto/cp932/utf-8/utf-8-sig)。

### Q-A2. 奉行のカラム順序が違うバージョンでも動きますか?

はい。スクリプトは **カラム名で値を取得**するため、列の並び順は関係ありません。例えば「日付」が 5 列目にあっても 1 列目にあっても同じように動きます。奉行のメジャー更新でカラム順が変わってもスクリプト改修は不要です。

### Q-A3. 必須カラムが欠落しているとどうなりますか?

スクリプトはエラー停止し、以下を表示します:
- 欠落している必須カラム名
- 検出された実際のヘッダー行 (デバッグ用)
- ネクストアクション (奉行の再エクスポート方法など)

必須カラム:`日付` / `伝票No.` / `借方勘定科目名` / `借方本体金額` / `貸方勘定科目名` / `貸方本体金額` の 6 つ。これらが無いと変換不能なので、奉行のエクスポート設定を確認して再エクスポートしてください。

### Q-A4. オプショナルカラムが無いとどうなりますか?

警告ログを出して処理は継続します。該当する freee 列が空欄になるだけです。

オプショナル例:
- `借方取引先名`/`借方取引先コード`/`貸方取引先名`/`貸方取引先コード` → freee 取引先列が空欄
- `借方部門名` → freee 借方部門が空欄
- `摘要` → freee 摘要が空欄

freee アップロード時にマッピング画面で対応するか、空欄のままインポートして freee 上で後付け編集も可能です。

### Q-A5. カラム名のスペルが微妙に違う場合は?

例えば「伝票No」(ピリオド無し) vs 「伝票No.」(ピリオド有り) のような微妙な違いがあると、スクリプトは別カラムと認識して必須欠落エラーになります。奉行 CSV のヘッダー名を確認し、必要なら手動でヘッダー行のみ修正してから実行してください。

スクリプトは前後の空白と BOM 文字は自動で除去するため、`  日付  ` のような空白付きは問題ありません。

---

## 設置・実行まわり

### Q1. スクリプトはどこのフォルダに置いても動きますか?

はい、どこに置いても動きます。スクリプト内部にハードコードされたファイルパスは一切なく、入出力はすべて CLI 引数で指定するため、デスクトップ、USB メモリ、共有ドライブ、どこでも実行可能です。Python 3.8 以上 が動く環境であれば OS も問いません (Windows / Mac / Linux)。

### Q2. Python のバージョン確認方法は?

ターミナル (Mac/Linux) またはコマンドプロンプト (Windows) で:

```
python3 --version
```

Windows で `python3` が見つからない場合は `python --version` を試してください。`Python 3.8.x` 以上であれば OK。古い場合は [README.md](README.md) の「2. Python のインストール」を参照。

### Q3. pip インストールが必要なライブラリはありますか?

ありません。標準ライブラリ (csv, argparse, os, re, sys, collections, datetime) のみで動きます。`pip install` 不要。

### Q4. 何度でも実行できますか?

はい。同じ出力先フォルダの同名ファイルは **上書き**されます。元の奉行 CSV や既存の freee 出力に副作用はありません。実行前のバックアップは不要ですが、不安なら別名フォルダで出力先を分ければ安全です。

### Q5. 1 回の実行にどれくらいかかりますか?

奉行 18,000 行程度であれば数秒〜十数秒で完了します。

---

## 取引先・補助科目まわり

### Q6. 取引先が空欄の行が多いのはなぜ?

3 つの正常な理由があります:

1. **奉行原本側で取引先が記録されていない仕訳** — 経費精算、内部振替、銀行手数料など。
2. **奉行原本で `その他取引先` / コード `000000` になっている仕訳** — 実質的に「取引先指定なし」なので空欄化しています (ノイズ除去)。
3. **「複合」行** — 借方/貸方の片側または両側を「複合」で補完した場合、その複合側の取引先は必ず空欄。これは freee 仕様準拠の正常な挙動。

実際の充足率は約 30% (借方) / 5% (貸方) です。

### Q7. 補助科目に取引先名が入っていません。これでいいですか?

はい。新スクリプトは **取引先を専用列 (「借方取引先」「借方取引先コード」「貸方取引先」「貸方取引先コード」) に入れる方式**にしています。freee 公式 33 列拡張テンプレートの正規ルートです。補助科目列は「品目的なもの」「口座的なもの」「補助分類名」のための列として使い分けられます。

### Q8. freee のマッピング画面で「補助科目をどれに紐づけるか」と聞かれます

新スクリプトでは取引先は専用列にあるため、補助科目列は本来の意味 (品目 / 補助分類) で使います。マッピングは:

- 補助科目で `振込`/`代引手数料`/`会社広告` 等の品目的なもの → **「品目」または「補助科目」** として登録
- 補助科目で `本社　みずほ川口2614625` のような口座名が混じっている場合 (`普通預金` 置換後ではなく元から補助科目にあったもの) → 状況に応じて

迷ったら「補助科目」として登録するのが無難です。

### Q9. 「マスタで未登録の取引先があります」とインポート画面で表示されます

問題ありません。freee は未登録取引先をインポート時に **新規登録 / スキップ / 既存に紐付け** から選ばせてくれます。「新規登録」を選べばその取引先がマスタに自動作成されます。取引先コードの設定はマスタ画面から後で追加してください。

### Q10. 254 件の取引先全部に確認画面が出るのは大変です

事前に freee の「取引先マスタ」画面でインポートしておく方法もあります。手順:

1. 奉行から取引先マスタを CSV でエクスポート
2. freee の「マスタ・口座 → 取引先」 → 「インポート」で取り込み
3. その後で本スクリプトの出力 CSV をインポートすると、マッチング済みで未登録扱いが減る

---

## 仕訳の中身まわり

### Q11. 「複合」勘定がたくさん出ていますが大丈夫ですか?

正常です。freee の振替伝票仕様では各行で借貸完全一致が必須で、奉行の「借方のみ」「貸方のみ」明細を表現するには反対側を「複合」で埋める必要があります。freee 公式 PDF にも「勘定科目『複合』でインポートすると年度締めができません。『諸口』などを用いてください」との注意がありますが、これは freee 上で別途「複合」勘定を「諸口」等にリネームすれば対応可能です。

### Q12. 「複合」を別の名前にしたいのですが

スクリプト内で `複合` を `諸口` などにグローバル置換するか、freee 取り込み後にマッピング画面で「複合」を「諸口」勘定にマッピングしてください。

### Q13. マイナス金額の仕訳 (返品) が混ざっていますが大丈夫ですか?

はい、freee 仕様で `-3575` のようなマイナス金額は許容されます (カンマ無しの符号付き整数)。スクリプトはこの形式で出力します。

ただし奉行原本の税区分が `課売上` のままマイナス金額で起票されている返品仕訳がある場合 (本来は `課売返` 区分が望ましい)、消費税申告で「課税売返」への振替が必要な可能性があります。これらは監査ログの「[要確認 2/2]」セクションに自動列挙されるので確認してください。

### Q14. 奉行原本の借貸不一致を直してくれますか?

直しません。スクリプトは奉行原本を忠実に変換するだけで、奉行起票時点の不一致は freee インポート後も合計差として残ります。不一致伝票は監査ログの「[要確認 1/2]」セクションに自動列挙されるので、経理担当が判断してください。

### Q15. 「両側データあり」行が 2 行に分解されると伝票が膨張します

意図通りです。例えば奉行 1 行 (借 普通預金 322,113 / 貸 売上 337,700) は freee で 2 行に分解されます (借 普通預金 / 貸 複合 と 借 複合 / 貸 売上)。これがないと借貸不一致になり freee エラーになります。

### Q16. 普通預金 → 補助科目名 への置換は何のため?

freee では「普通預金」は **口座マスタの 1 口座** として表現します。奉行で「借方 普通預金 / 補助科目 本社　みずほ川口2614625」のような起票は、freee では「借方 本社　みずほ川口2614625」(口座マスタを直接指定) として表現するのが自然です。スクリプトはこの変換を自動で行います。

---

## ファイル分割まわり

### Q17. 上下分割を 1 ファイルにまとめるには?

`--rows-per-file 50000` のように freee 上限 (50,000 行) より大きい値を指定すれば、1 ファイル出力になります。

```
python3 obc_to_freee.py --input 上.csv 下.csv --output-dir ./output --output-prefix freee_all --rows-per-file 50000
```

### Q18. 分割行数のしきい値はいくらが推奨?

freee 公式上限が 50,000 行。マッピング画面の操作性を考えると **10,000 行程度** が扱いやすいです。20,000 行を超えるとマッピング画面の表示が重くなる場合があります。

### Q19. ファイル分割の境界はどう決まりますか?

伝票境界優先。例えば `--rows-per-file 10000` を指定しても、10,000 行目が伝票途中なら、その伝票の最終行までは現ファイルに含めてから次ファイルへ移ります。同じ (日付, 伝票番号) が複数ファイルに分かれることはありません。

### Q20. アップロード順序は決まっていますか?

`_001` → `_002` → `_003` の順番でアップロードしてください。順番が違うと伝票が分断される場合があります (同じ日付・伝票番号が後のファイルでまた現れると、freee が「重複伝票」エラーで弾く場合がある)。

---

## 期間カットまわり

### Q21. 期間カットの実行例は?

```
python3 obc_to_freee.py \
  --input 上.csv 下.csv \
  --output-dir ./output \
  --output-prefix freee用_11月分のみ \
  --from 2025/11/01 \
  --to 2025/11/30
```

`--from` のみ、`--to` のみの指定も可能 (片端のみカット)。

### Q22. 期間カットすると監査ログの件数も変わりますか?

はい。期間カット範囲内の伝票だけを集計します。全期間で N 件 + M 件の検出が、`--from` `--to` 指定によってその範囲内のみに絞られて表示されます (件数は対象データに依存。例: 全期間で 6 件 + 6 件 → `--from 2025/11/01 --to 2026/01/31` で 3 件 + 2 件)。

### Q23. 月別に分けたい

1 月分、2 月分… と複数回スクリプトを実行すれば、月別ファイルが作れます。各回で `--output-prefix` を変えてください:

```
python3 obc_to_freee.py --input ... --output-dir ./output --output-prefix freee用_2025年11月 --from 2025/11/01 --to 2025/11/30
python3 obc_to_freee.py --input ... --output-dir ./output --output-prefix freee用_2025年12月 --from 2025/12/01 --to 2025/12/31
python3 obc_to_freee.py --input ... --output-dir ./output --output-prefix freee用_2026年01月 --from 2026/01/01 --to 2026/01/31
```

---

## 監査ログまわり

### Q24. 監査ログを画面に出さずファイルに保存したい

Mac/Linux:

```
python3 obc_to_freee.py [引数] 2> 監査ログ.txt
```

Windows コマンドプロンプト:

```
python obc_to_freee.py [引数] 2> 監査ログ.txt
```

`2>` は stderr リダイレクト。stdout (進捗) は画面に出続けます。

> ⚠️ `2>` で保存される内容は監査ログ (`[要確認 1/2]`/`[要確認 2/2]`) だけでなく、エラーレポート (税区分・金額エラー等)・整合性チェック結果 (WARN/PASS/ERROR) も含む全 stderr 出力です。

### Q25. 監査ログを完全に抑制したい

`--quiet-audit` フラグを追加してください:

```
python3 obc_to_freee.py [引数] --quiet-audit
```

ただし、奉行原本起因の問題を見逃すリスクがあるため、初回実行時は必ず監査ログを確認することを推奨します。

> ⚠️ ただしエラーレポート (税区分・金額エラー等) と整合性チェック結果 (借貸合計の WARN/PASS/ERROR) は `--quiet-audit` では抑制されず、引き続き stderr に出力されます。これらは変換品質を担保するため常時表示されます。

### Q26. 監査ログの「要確認 1/2」は freee アップロード時のエラーになりますか?

なりません。freee 行単位では借貸一致するよう自動補完されるため、freee は受け入れます。ただし伝票単位で借方合計 ≠ 貸方合計のままになるため、freee 上の **試算表で合計が合わない** ケースが発生する可能性があります。経理担当が判断して、奉行側で修正するか freee 上で別途調整仕訳を入れてください。

### Q27. 監査ログ「要確認 2/2」の件数を減らすには?

奉行側で返品仕訳の税区分を `課売上` から `課売返` に振り替えてからエクスポートし直すと、スクリプトが自動で freee の正式コード `課税売返10%` (消費税申告区分名:「課税売上 − 返還等」) に変換します。

---

## エラー・トラブル

### Q28. 「想定外の税区分が見つかりました」とエラーレポートに表示された

奉行原本に変換マッピング未登録の税区分 + 税率の組み合わせが含まれています。スクリプトは未知の税区分を「対象外」で仮置きして変換を継続し、実行後のエラーレポートに検出パターン・件数・該当伝票例・ネクストアクションを一覧表示します。

対処方法:
- **方法 A (推奨)**: 奉行で該当伝票の税区分を freee に存在する区分に修正し、CSV を再エクスポートして再実行
- **方法 B**: スクリプトの `TAX_MAP` 辞書 (`obc_to_freee.py` ファイル冒頭) に該当エントリを追加して再実行

⚠️ 仮置き「対象外」のまま freee へアップロードすると、消費税申告に支障が出る可能性があります。

### Q29. 文字化けして見えます

スクリプトは CP932 / UTF-8 / UTF-8 BOM / UTF-16 を自動判定するため、奉行 CSV のエンコーディングが原因で失敗することは通常ありません。もし「エンコーディングを判定できませんでした」エラーが出る場合は、奉行 CSV を CP932 (Shift-JIS) または UTF-8 で再保存してから再実行してください。`--encoding` フラグでも明示指定できますが、自動判定が失敗する状況では明示指定でも読み込めない可能性が高いです。出力 CSV を Excel で開いた時に文字化けする場合は UTF-8 BOM 付きなので Excel 2016 以降なら正しく開けます。

### Q30. `FileNotFoundError` が出ます

`--input` で指定したファイル名と、実際のファイル名が一致していません。`dir` (Windows) / `ls` (Mac/Linux) でフォルダ内のファイル名を確認してください。日本語ファイル名のため、コピペで指定するのが安全です。

### Q31. freee で「マスタで未登録の勘定科目があります」と出る

スクリプトは奉行の勘定科目名をそのまま使います。freee 側に該当勘定科目がない場合、インポート時に「新規登録 / 既存にマッピング」を選べます。よく出るパターン:

- 奉行: 「メーカー＆ＳＰ　りそな川口4799601」(普通預金の補助科目を勘定科目化したもの) → freee で「口座」マスタとして新規登録
- 奉行: 「会員カード売上」 → freee の「売上高」配下の補助科目として新規登録

### Q32. ファイルサイズが 64MB を超えそうです

`--rows-per-file 10000` で十分小さく分割されるはずですが、もし超える場合は `--rows-per-file 5000` のように小さくしてください。本変換では 18,000 行で約 3.6MB 程度なので、通常は問題ありません。

---

## ロールバック・やり直し

### Q33. freee に間違ったファイルを取り込んでしまいました

freee の「振替伝票 → インポート → 仕訳インポート履歴」を開き、該当のインポート行の「削除」ボタンをクリックすると、インポート由来の振替伝票が一括削除されます。ただし freee 上で手動編集した伝票も巻き込まれて削除される場合があるため、慎重に。

### Q34. ロールバック後に正しいファイルでやり直したい

`output` フォルダの正しい CSV を `_001` から順にアップロードすれば OK。マッピング設定が前回と同じなら、freee は前回の設定を覚えていてスキップできます (「過去にインポートした勘定科目・補助科目・税区分と異なる内容でインポートする場合は、科目のインポート設定から過去のインポート内容を削除してください」と画面に出る場合は指示通り)。

### Q35. ある月だけやり直したい

該当月だけ期間カットでエクスポートして freee にインポート。事前に freee 上で該当月の振替伝票を削除しておく必要があります。重複インポートになると freee はエラーで弾きます。

---

## その他

### Q36. 奉行のエクスポート形式が変わったらどうなりますか?

スクリプトは **カラム名ベース**で値を取得するため、列順の入れ替えや、新しい列の追加には影響を受けません。必須カラム (日付・伝票No.・借方/貸方勘定科目名・借方/貸方本体金額) のいずれかが消えるとエラー停止し、欠落カラム名とネクストアクションが表示されます。オプショナルカラム (取引先・部門・補助科目・税区分・税率・摘要・消費税額) は欠落しても警告のみで処理継続します。逆に新しい必須カラムをスクリプト側で要求したい場合は `REQUIRED_COLS` リストに追加してください。

### Q37. freee 33 列フォーマットが将来変わったら?

`FREEE_HEADER` 配列を更新してください。列が増減する場合は出力行の組み立て箇所も修正必要。

### Q38. このスクリプトを別の会計ソフト (弥生、MFクラウド) で使えますか?

そのままでは使えません。入力は奉行のカラム名 (例: `日付`、`借方勘定科目名`、`借方本体金額` 等) を前提にし、出力は freee 33 列拡張テンプレート固定です。他社ソフトの CSV カラム名・税区分体系・金額表現が異なる場合は別途変換ロジックが必要です。エンコーディング自動判定とカラム名参照の仕組みは流用可能なので、`OBC_COL_*` 定数群と `TAX_MAP` を差し替えれば他ソフト用バリアントを作りやすい構造になっています。

### Q39. デバッグしたい

`--from` `--to` で 1 日分程度に絞って実行すると、変換結果を素早く確認できます。出力 CSV を Excel や Numbers で開いて目視確認するのが効率的です。

### Q40. データ移行で失敗が怖いです

以下の手順を推奨します:

1. 奉行から **1 日分だけ** エクスポート (または `--from`/`--to` で絞る)
2. このスクリプトで変換
3. freee の **テスト用事業所** (または無料プランの別事業所) を作成
4. テスト事業所で取り込んで結果を確認
5. 問題なければ本番事業所で本実行

freee の「事業所」は無料プランで作れるため、安全な検証環境として活用できます。
