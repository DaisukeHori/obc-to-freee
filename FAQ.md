# FAQ — obc_to_freee.py よくある質問

## 入力ファイル形式まわり

### Q-A1. 奉行 CSV が UTF-8 形式でも読めますか?

はい。**CP932 (Shift-JIS) / UTF-8 / UTF-8 BOM / UTF-16** をすべて自動判定して読み込みます。何も指定しなくても自動でエンコーディングを認識します。明示指定したい場合は `--encoding cp932` のように指定可能です (`auto` / `cp932` / `utf-8` / `utf-8-sig` から選択)。

※ UTF-16 は自動判定のみ対応。`--encoding utf-16` のような明示指定は不可です (`--encoding` の選択肢は auto/cp932/utf-8/utf-8-sig)。

### Q-A2. 奉行のカラム順序が違うバージョンでも動きますか?

はい。スクリプトは **カラム名で値を取得**するため、列の並び順は関係ありません。例えば「日付」が 5 列目にあっても 1 列目にあっても同じように動きます。奉行のメジャー更新でカラム順が変わってもスクリプト改修は不要です。

### Q-A3. 必須カラムが欠落しているとどうなりますか?

スクリプトはエラー停止し、以下を表示します:
- 欠落している必須カラム名
- 検出された実際のヘッダー行 (デバッグ用)
- ネクストアクション (奉行の再エクスポート方法など)

必須カラム:`日付` / `伝票No.` / `借方勘定科目名` / `借方本体金額` / `貸方勘定科目名` / `貸方本体金額` の 6 つ。これらが無いと変換不能なので、奉行のエクスポート設定を確認して再エクスポートしてください。

### Q-A4. オプショナルカラムが無いとどうなりますか?

警告ログを出して処理は継続します。該当する freee 列が空欄になるだけです。

オプショナル例:
- `借方取引先名`/`借方取引先コード`/`貸方取引先名`/`貸方取引先コード` → freee 取引先列が空欄
- `借方部門名` → freee 借方部門が空欄
- `摘要` → freee 摘要が空欄

freee アップロード時にマッピング画面で対応するか、空欄のままインポートして freee 上で後付け編集も可能です。

### Q-A5. カラム名のスペルが微妙に違う場合は?

例えば「伝票No」(ピリオド無し) vs 「伝票No.」(ピリオド有り) のような微妙な違いがあると、スクリプトは別カラムと認識して必須欠落エラーになります。奉行 CSV のヘッダー名を確認し、必要なら手動でヘッダー行のみ修正してから実行してください。

スクリプトは前後の空白と BOM 文字は自動で除去するため、`  日付  ` のような空白付きは問題ありません。

---

## 設置・実行まわり

### Q1. スクリプトはどこのフォルダに置いても動きますか?

はい、どこに置いても動きます。スクリプト内部にハードコードされたファイルパスは一切なく、入出力はすべて CLI 引数で指定するため、デスクトップ、USB メモリ、共有ドライブ、どこでも実行可能です。Python 3.8 以上 が動く環境であれば OS も問いません (Windows / Mac / Linux)。

### Q2. Python のバージョン確認方法は?

ターミナル (Mac/Linux) またはコマンドプロンプト (Windows) で:

```
python3 --version
```

Windows で `python3` が見つからない場合は `python --version` を試してください。`Python 3.8.x` 以上であれば OK。古い場合は [README.md](README.md) の「2. Python のインストール」を参照。

### Q3. pip インストールが必要なライブラリはありますか?

ありません。標準ライブラリ (csv, argparse, os, re, sys, collections, datetime) のみで動きます。`pip install` 不要。

### Q4. 何度でも実行できますか?

はい。同じ出力先フォルダの同名ファイルは **上書き**されます。元の奉行 CSV や既存の freee 出力に副作用はありません。実行前のバックアップは不要ですが、不安なら別名フォルダで出力先を分ければ安全です。

### Q5. 1 回の実行にどれくらいかかりますか?

奉行 18,000 行程度であれば数秒〜十数秒で完了します。

---

## 取引先・補助科目まわり

### Q6. 取引先が空欄の行が多いのはなぜ?

3 つの正常な理由があります:

1. **奉行原本側で取引先が記録されていない仕訳** — 経費精算、内部振替、銀行手数料など。
2. **奉行原本で `その他取引先` / コード `000000` になっている仕訳** — 実質的に「取引先指定なし」なので空欄化しています (ノイズ除去)。
3. **振替伝票形式の片側空欄行** — 奉行の借方または貸方のみ有効な明細を振替伝票形式に変換した場合、非該当側の取引先は必ず空欄。これは freee 公式推奨の振替伝票形式による正常な挙動。

実際の充足率は約 30% (借方) / 5% (貸方) です。

### Q7. 補助科目に取引先名が入っていません。これでいいですか?

はい。新スクリプトは **取引先を専用列 (「借方取引先」「借方取引先コード」「貸方取引先」「貸方取引先コード」) に入れる方式**にしています。freee 公式 33 列拡張テンプレートの正規ルートです。補助科目列は「品目的なもの」「口座的なもの」「補助分類名」のための列として使い分けられます。

### Q8. freee のマッピング画面で「補助科目をどれに紐づけるか」と聞かれます

新スクリプトでは取引先は専用列にあるため、補助科目列は本来の意味 (品目 / 補助分類) で使います。マッピングは:

- 補助科目で `振込`/`代引手数料`/`会社広告` 等の品目的なもの → **「品目」または「補助科目」** として登録
- 補助科目で `本社　みずほ川口2614625` のような口座名が混じっている場合 (`普通預金` 置換後ではなく元から補助科目にあったもの) → 状況に応じて

迷ったら「補助科目」として登録するのが無難です。

### Q9. 「マスタで未登録の取引先があります」とインポート画面で表示されます

問題ありません。freee は未登録取引先をインポート時に **新規登録 / スキップ / 既存に紐付け** から選ばせてくれます。「新規登録」を選べばその取引先がマスタに自動作成されます。取引先コードの設定はマスタ画面から後で追加してください。

### Q10. 254 件の取引先全部に確認画面が出るのは大変です

`--output-partners` フラグを使うと、奉行原本から取引先マスタ CSV を自動生成して freee に事前取り込みできます。これにより仕訳インポート時の「未登録取引先」確認画面がほぼなくなります。

**推奨手順:**

1. 仕訳変換コマンドに `--output-partners` を追加して実行:

   ```
   python3 obc_to_freee.py --input 上.csv 下.csv --output-dir output \
     --output-prefix freee用_仕訳データ_obc変換 --rows-per-file 10000 \
     --output-partners
   ```

2. `output/freee取引先マスタ.csv` が生成される

3. freee の **マスタ・口座 → 取引先 → インポート** を開き、生成された CSV を取り込む  
   → 「**取引先コードを元にインポート**」を選択

4. 取引先マスタ取り込み完了後に、仕訳 CSV (`freee用_仕訳データ_obc変換_001.csv` 等) をインポートする

> 詳細は [README 9-10 取引先マスタ CSV](README.md#9-10-取引先マスタ-csv) を参照。

---

## 仕訳の中身まわり

### Q11. freee でインポートした後に「複合」科目が出ないですか?

出ません。本スクリプトは freee 公式推奨の **振替伝票形式 (片側空欄行)** で出力します。同一伝票 No の複数行で借方/貸方を表現するため、freee に取り込んだ後も「複合」科目は一切出ません。

freee 公式 PDF が「勘定科目『複合』でインポートすると年度締めができません。『諸口』などを用いてください」と警告しているため、本スクリプトはこの警告を回避する形式を採用しています。片側 (非該当側) の勘定科目・補助科目・税区分・金額・取引先はすべて空欄になります。

### Q12. 以前のバージョンで出力した CSV に「複合」科目が含まれています

手動置換は不要です。本バージョンから振替伝票形式 (片側空欄) に変更されたため、新たに変換し直してください。再変換後の出力には「複合」科目は一切含まれません。

**既に旧 CSV (複合形式) を freee に取り込んでしまった場合のロールバック手順:**

1. **旧データの削除**: freee の「振替伝票 → インポート → 仕訳インポート履歴」を開き、旧 CSV に対応するインポート行の「削除」ボタンをクリックします。これにより、そのインポート由来の振替伝票が一括削除されます (詳細は [Q33](#q33-freee-に間違ったファイルを取り込んでしまいました) を参照)。
2. **再変換**: 本バージョンのツールで奉行原本 CSV を再変換し、振替伝票形式の新 CSV を生成します。
3. **再インポート**: 新 CSV を `_001` から順に freee へアップロードします。

**注意**: 旧データを削除せずに新 CSV をインポートすると、**重複仕訳**になります。旧 CSV (複合形式) と新 CSV (振替伝票形式) は「同一日付・伝票No・金額の完全一致」とはみなされないため、freee の重複防止フィルタが機能しません。必ず先に旧データを削除してから再インポートしてください。

### Q13. マイナス金額の仕訳 (返品) が混ざっていますが大丈夫ですか?

はい、freee 仕様で `-3575` のようなマイナス金額は許容されます (カンマ無しの符号付き整数)。スクリプトはこの形式で出力します。

ただし奉行原本の税区分が `課売上` のままマイナス金額で起票されている返品仕訳がある場合 (本来は `課売返` 区分が望ましい)、消費税申告で「課税売返」への振替が必要な可能性があります。これらは監査ログの「[要確認] 課売上マイナス」セクションに自動列挙されるので確認してください。

### Q14. 奉行原本の借貸不一致を直してくれますか?

直しません。スクリプトは奉行原本を忠実に変換するだけで、奉行起票時点の不一致は freee インポート後も合計差として残ります。不一致伝票は監査ログの「[要確認] 借貸不一致」セクションに自動列挙されるので、経理担当が判断してください。

### Q15. 「両側データあり」行が 2 行に分解されると伝票が膨張します

意図通りです。例えば奉行 1 行 (借 普通預金 322,113 / 貸 売上 337,700) は freee で 2 行に分解されます (借 普通預金 [貸方空欄] と [借方空欄] 貸 売上)。振替伝票形式では同一伝票 No の行を合算して借貸を確認するため、伝票単位の借貸合計は一致します。

### Q16. 普通預金 → 補助科目名 への置換は何のため?

freee では「普通預金」は **口座マスタの 1 口座** として表現します。奉行で「借方 普通預金 / 補助科目 本社　みずほ川口2614625」のような起票は、freee では「借方 本社　みずほ川口2614625」(口座マスタを直接指定) として表現するのが自然です。スクリプトはこの変換を自動で行います。

---

## ファイル分割まわり

### Q17. 上下分割を 1 ファイルにまとめるには?

`--rows-per-file 50000` のように freee 上限 (50,000 行) より大きい値を指定すれば、1 ファイル出力になります。

```
python3 obc_to_freee.py --input 上.csv 下.csv --output-dir ./output --output-prefix freee_all --rows-per-file 50000
```

### Q18. 分割行数のしきい値はいくらが推奨?

freee 公式上限が 50,000 行。マッピング画面の操作性を考えると **10,000 行程度** が扱いやすいです。20,000 行を超えるとマッピング画面の表示が重くなる場合があります。

### Q19. ファイル分割の境界はどう決まりますか?

`--rows-per-file 10000` 指定時、**各ファイルは 10,000 行 (データ行) を超えません**。次伝票を加えるとしきい値を超える場合は、その伝票全体を次ファイルの先頭に送ります。これにより:

- 上限厳守 (各ファイルは ヘッダー 1 行 + データ 10,000 行 = 最大 10,001 行)
- 伝票境界をまたがない (同じ日付・伝票番号が複数ファイルに分かれない)

**例外**: 1 伝票単体で `--rows-per-file` を超える場合のみ、伝票境界を保つために 1 ファイルに収め、stderr に警告が出ます (奉行で 1 伝票に大量明細を持つ稀なケース)。

```
WARN: 1 伝票で 12000 行あり、--rows-per-file (10000) を超えています。
伝票境界を保つため 1 ファイルに収めます。
```

このような警告が出た場合は奉行側で伝票を分割するか、`--rows-per-file` を大きくしてください。

### Q20. アップロード順序は決まっていますか?

`_001` → `_002` → `_003` の順番でアップロードしてください。順番が違うと伝票が分断される場合があります (同じ日付・伝票番号が後のファイルでまた現れると、freee が「重複伝票」エラーで弾く場合がある)。

---

## 期間カットまわり

### Q21. 期間カットの実行例は?

```
python3 obc_to_freee.py \
  --input 上.csv 下.csv \
  --output-dir ./output \
  --output-prefix freee用_11月分のみ \
  --from 2025/11/01 \
  --to 2025/11/30
```

`--from` のみ、`--to` のみの指定も可能 (片端のみカット)。

### Q22. 期間カットすると監査ログの件数も変わりますか?

はい。期間カット範囲内の伝票だけを集計します。全期間で N 件 + M 件の検出が、`--from` `--to` 指定によってその範囲内のみに絞られて表示されます (件数は対象データに依存。例: 全期間で 6 件 + 6 件 → `--from 2025/11/01 --to 2026/01/31` で 3 件 + 2 件)。

### Q23. 月別に分けたい

1 月分、2 月分… と複数回スクリプトを実行すれば、月別ファイルが作れます。各回で `--output-prefix` を変えてください:

```
python3 obc_to_freee.py --input ... --output-dir ./output --output-prefix freee用_2025年11月 --from 2025/11/01 --to 2025/11/30
python3 obc_to_freee.py --input ... --output-dir ./output --output-prefix freee用_2025年12月 --from 2025/12/01 --to 2025/12/31
python3 obc_to_freee.py --input ... --output-dir ./output --output-prefix freee用_2026年01月 --from 2026/01/01 --to 2026/01/31
```

---

## 監査ログまわり

### Q24. 監査ログを画面に出さずファイルに保存したい

Mac/Linux:

```
python3 obc_to_freee.py [引数] 2> 監査ログ.txt
```

Windows コマンドプロンプト:

```
python obc_to_freee.py [引数] 2> 監査ログ.txt
```

`2>` は stderr リダイレクト。stdout (進捗) は画面に出続けます。

> ⚠️ `2>` で保存される内容は監査ログ (`[要確認] 借貸不一致`/`[要確認] 課売上マイナス`/`[要確認] 非課売上+税額あり`/`[業務確認用]`) だけでなく、エラーレポート (税区分・金額エラー等)・整合性チェック結果 (WARN/PASS/ERROR) も含む全 stderr 出力です。

### Q25. 監査ログを完全に抑制したい

`--quiet-audit` フラグを追加してください:

```
python3 obc_to_freee.py [引数] --quiet-audit
```

ただし、奉行原本起因の問題を見逃すリスクがあるため、初回実行時は必ず監査ログを確認することを推奨します。

> ⚠️ ただしエラーレポート (税区分・金額エラー等) と整合性チェック結果 (借貸合計の WARN/PASS/ERROR) は `--quiet-audit` では抑制されず、引き続き stderr に出力されます。これらは変換品質を担保するため常時表示されます。

### Q26. 監査ログの「要確認 借貸不一致」は freee アップロード時のエラーになりますか?

なりません。freee 行単位では借貸一致するよう自動補完されるため、freee は受け入れます。ただし伝票単位で借方合計 ≠ 貸方合計のままになるため、freee 上の **試算表で合計が合わない** ケースが発生する可能性があります。経理担当が判断して、奉行側で修正するか freee 上で別途調整仕訳を入れてください。

### Q27. 監査ログ「要確認 課売上マイナス」の件数を減らすには?

奉行側で返品仕訳の税区分を `課売上` から `課売返` に振り替えてからエクスポートし直すと、スクリプトが自動で freee の正式コード `課税売返10%` (消費税申告区分名:「課税売上 − 返還等」) に変換します。

### Q27a. 監査ログ「要確認 非課売上+税額あり」が表示されました。どう対処すれば?

「要確認 非課売上+税額あり」は、奉行の税区分が「非売上」(非課税売上) なのに税額が入っている矛盾起票です。freee は「非課売上では税額を入力できません」エラーで弾くため、以下のいずれかで対処してください:

1. **推奨**: 奉行で該当伝票の税区分を「課売上」に修正して CSV を再エクスポートし再実行
2. `--non-taxable-mismatch-strategy=zero-tax` で税額を 0 に修正 (freee エラー回避、消費税控除なし)
3. `--non-taxable-mismatch-strategy=change-to-taxable` で税区分を「課税売上10%」に自動変更 (税控除も取れる)

### Q27b. 監査ログ「参考情報 (業務確認用)」が表示されました。freee エラーになりますか? 要確認件数に含まれますか?

なりません。また **GUI の「要確認件数」サマリには算入しません**。対処は任意です。

「業務確認用」は奉行の税区分「非仕入」が freee の「非対仕入10%」等に自動変換されるため、freee インポートは成功します (※「非対仕入10%」への変換可否は freee 実機での確認を推奨)。ただし奉行側の起票内容が意図通りかを業務的に確認してください:

- **非課税売上対応の課税仕入** (住宅家賃に対する清掃外注費・修繕費等) → 非対仕入10% は正しい変換です。そのまま freee にインポートしてください。
- **本来税額のかからない非課税仕入** (利息・保険料・社会保険料等) → 奉行の税区分を「対象外」または「非課税」に修正し、再エクスポートを推奨します。

### Q27c. 以前 freee で「非課仕入では税額を入力できません」エラーが出ていた仕訳が、今回エラーが出なくなったのはなぜ?

奉行の「非仕入」を従来は freee の「非課仕入」(非課税取引における仕入) に変換していましたが、業務的には「非対仕入」(非課税売上に対応する課税仕入) が正解でした。本ツールは「非対仕入10%/8%（軽）/対象外」に税率別変換するよう修正しており、freee で取込成功するようになりました。なお変換結果は監査ログの「[業務確認用]」セクションに一覧表示されますので、業務的な妥当性確認にご利用ください。

---

### Q27d. 仕入返品 (奉行で課仕返/共仕返) はどう変換されますか?

奉行の税区分 `課仕返` は、税率に応じて freee の以下コードに変換されます:

| 奉行 | 税率 | → freee |
|------|------|---------|
| 課仕返 | 10% | 課対仕返10% |
| 課仕返 | 8%  | 課対仕返8%（軽） |
| 課仕返 | 0/空欄 | 対象外 |

`共仕返` (共通仕入返品) も同様です:

| 奉行 | 税率 | → freee |
|------|------|---------|
| 共仕返 | 10% | 共対仕返10% |
| 共仕返 | 8%  | 共対仕返8%（軽） |
| 共仕返 | 0/空欄 | 対象外 |

税率 `0` または空欄の場合は `対象外` として処理します。

---

## エラー・トラブル

### Q28. 「想定外の税区分が見つかりました」とエラーレポートに表示された

奉行原本に変換マッピング未登録の税区分 + 税率の組み合わせが含まれています。スクリプトは未知の税区分を「対象外」で仮置きして変換を継続し、実行後のエラーレポートに検出パターン・件数・該当伝票例・ネクストアクションを一覧表示します。

対処方法:
- **方法 A (推奨)**: 奉行で該当伝票の税区分を freee に存在する区分に修正し、CSV を再エクスポートして再実行
- **方法 B**: スクリプトの `TAX_MAP` 辞書 (`obc_to_freee.py` ファイル冒頭) に該当エントリを追加して再実行

⚠️ 仮置き「対象外」のまま freee へアップロードすると、消費税申告に支障が出る可能性があります。

### Q29. 文字化けして見えます

スクリプトは CP932 / UTF-8 / UTF-8 BOM / UTF-16 を自動判定するため、奉行 CSV のエンコーディングが原因で失敗することは通常ありません。もし「エンコーディングを判定できませんでした」エラーが出る場合は、奉行 CSV を CP932 (Shift-JIS) または UTF-8 で再保存してから再実行してください。`--encoding` フラグでも明示指定できますが、自動判定が失敗する状況では明示指定でも読み込めない可能性が高いです。出力 CSV を Excel で開いた時に文字化けする場合は UTF-8 BOM 付きなので Excel 2016 以降なら正しく開けます。

### Q30. `FileNotFoundError` が出ます

`--input` で指定したファイル名と、実際のファイル名が一致していません。`dir` (Windows) / `ls` (Mac/Linux) でフォルダ内のファイル名を確認してください。日本語ファイル名のため、コピペで指定するのが安全です。

### Q31. freee で「マスタで未登録の勘定科目があります」と出る

スクリプトは奉行の勘定科目名をそのまま使います。freee 側に該当勘定科目がない場合、インポート時に「新規登録 / 既存にマッピング」を選べます。よく出るパターン:

- 奉行: 「メーカー＆ＳＰ　りそな川口4799601」(普通預金の補助科目を勘定科目化したもの) → freee で「口座」マスタとして新規登録
- 奉行: 「会員カード売上」 → freee の「売上高」配下の補助科目として新規登録

### Q32. ファイルサイズが 64MB を超えそうです

`--rows-per-file 10000` で十分小さく分割されるはずですが、もし超える場合は `--rows-per-file 5000` のように小さくしてください。本変換では 18,000 行で約 3.6MB 程度なので、通常は問題ありません。

---

## ロールバック・やり直し

### Q33. freee に間違ったファイルを取り込んでしまいました

freee の「振替伝票 → インポート → 仕訳インポート履歴」を開き、該当のインポート行の「削除」ボタンをクリックすると、インポート由来の振替伝票が一括削除されます。ただし freee 上で手動編集した伝票も巻き込まれて削除される場合があるため、慎重に。

### Q34. ロールバック後に正しいファイルでやり直したい

`output` フォルダの正しい CSV を `_001` から順にアップロードすれば OK。マッピング設定が前回と同じなら、freee は前回の設定を覚えていてスキップできます (「過去にインポートした勘定科目・補助科目・税区分と異なる内容でインポートする場合は、科目のインポート設定から過去のインポート内容を削除してください」と画面に出る場合は指示通り)。

### Q35. ある月だけやり直したい

該当月だけ期間カットでエクスポートして freee にインポート。事前に freee 上で該当月の振替伝票を削除しておく必要があります。重複インポートになると freee はエラーで弾きます。

---

## その他

### Q36. 奉行のエクスポート形式が変わったらどうなりますか?

スクリプトは **カラム名ベース**で値を取得するため、列順の入れ替えや、新しい列の追加には影響を受けません。必須カラム (日付・伝票No.・借方/貸方勘定科目名・借方/貸方本体金額) のいずれかが消えるとエラー停止し、欠落カラム名とネクストアクションが表示されます。オプショナルカラム (取引先・部門・補助科目・税区分・税率・摘要・消費税額) は欠落しても警告のみで処理継続します。逆に新しい必須カラムをスクリプト側で要求したい場合は `REQUIRED_COLS` リストに追加してください。

### Q37. freee 33 列フォーマットが将来変わったら?

`FREEE_HEADER` 配列を更新してください。列が増減する場合は出力行の組み立て箇所も修正必要。

### Q38. このスクリプトを別の会計ソフト (弥生、MFクラウド) で使えますか?

そのままでは使えません。入力は奉行のカラム名 (例: `日付`、`借方勘定科目名`、`借方本体金額` 等) を前提にし、出力は freee 33 列拡張テンプレート固定です。他社ソフトの CSV カラム名・税区分体系・金額表現が異なる場合は別途変換ロジックが必要です。エンコーディング自動判定とカラム名参照の仕組みは流用可能なので、`OBC_COL_*` 定数群と `TAX_MAP` を差し替えれば他ソフト用バリアントを作りやすい構造になっています。

### Q39. デバッグしたい

`--from` `--to` で 1 日分程度に絞って実行すると、変換結果を素早く確認できます。出力 CSV を Excel や Numbers で開いて目視確認するのが効率的です。

### Q40. データ移行で失敗が怖いです

以下の手順を推奨します:

1. 奉行から **1 日分だけ** エクスポート (または `--from`/`--to` で絞る)
2. このスクリプトで変換
3. freee の **テスト用事業所** (または無料プランの別事業所) を作成
4. テスト事業所で取り込んで結果を確認
5. 問題なければ本番事業所で本実行

freee の「事業所」は無料プランで作れるため、安全な検証環境として活用できます。

---

## 取引先マスタまわり

### Q-B1. 取引先マスタ CSV を生成するには?

変換コマンドに `--output-partners` フラグを追加するだけです:

```
python3 obc_to_freee.py \
  --input 奉行_上.csv 奉行_下.csv \
  --output-dir output \
  --output-prefix freee用_仕訳データ_obc変換 \
  --rows-per-file 10000 \
  --output-partners
```

実行後、`output/freee取引先マスタ.csv` が生成されます (ファイル名を変えたい場合は `--partners-prefix 任意の名前` を追加)。

出力 CSV は freee 公式「取引先インポート用テンプレート」と同じ 57 列構成・UTF-8 BOM 付きです。

### Q-B2. 取引先マスタはいつ取り込めばいい?

**仕訳 CSV より前に取り込んでください。**

仕訳 CSV を先に取り込むと「マスタで未登録の取引先があります」と全件確認画面が出て手間がかかります。取引先マスタを先に取り込んでおけば仕訳インポート時のマッチングが自動で完了します。

freee での取り込み手順:

1. **マスタ・口座 → 取引先** を開く
2. 画面右上の **インポート** ボタンをクリック
3. `freee取引先マスタ.csv` を選択
4. 「**取引先コードを元にインポート**」を選択 ← ここが重要
5. 「インポートを実行」をクリック
6. 完了後、仕訳 CSV をインポートする

### Q-B3. 同じ取引先コードに複数の名前が出てきた場合は? (セクション A)

奉行原本で同一コードの取引先に複数の表記が混在していた場合 (例: `堀 大輔` と `堀　大輔` (全角空白)) は、スクリプトが **出現回数最多の名前** を自動採用し、stderr に以下の警告を出力します:

```
======================================================================
[要確認 取引先マスタ A] 同一コードに複数の名前が見つかった取引先
======================================================================
  コード 010007: '堀 大輔' (5 件) / '堀　大輔' (3 件) → '堀 大輔' を採用
  合計 1 件
======================================================================
```

出現回数が **同数 (tie)** の場合は、奉行原本の出現順 (上のファイル → 下のファイル、各ファイル内は行番号順) で **最初に登場した名前** を採用します。

freee 取り込み前に経理担当が確認し、正式な表記に手動修正するか、採用名のまま進むか判断してください。

### Q-B4. 同じ取引先名で複数のコードがある場合は? (セクション B)

奉行原本で同一の取引先名に複数のコードが割り当てられている場合 (旧コード + 新コードが両方残っている等の名寄せ未実施ケース) は、stderr に以下の警告を出力します:

```
======================================================================
[要確認 取引先マスタ B] 同一名に複数のコードが見つかった取引先 (合計 3 件)
======================================================================
以下の取引先は同じ名前で複数のコードを持っています。
freee に取り込むと同名の取引先が複数登録される状態になります。
奉行原本側で名寄せを行うか、freee 取り込み後に手動で統合してください。

  名前 'エクシードシステム㈱': コード 742001 / 746209
  名前 'リコーリース㈱': コード 752002 / 753311
  名前 '㈱JCB': コード 753414 / 782536

  合計 3 件
======================================================================
```

**セクション A との違い**:
- **A** = 同一コードに複数の名前 → スクリプトが最頻名を自動採用して 1 行に統合 (経理担当の確認は推奨だが、出力 CSV は 1 行)
- **B** = 同一名に複数のコード → デフォルト (`warn-only`) ではそのまま複数行として出力 (freee 側で同名の取引先が複数登録される)

**対処方法 (B の場合)**:
- `--dedup-strategy` フラグで自動対処 (→ Q-B6/B7 参照)
- 奉行側で取引先マスタを統合してから再エクスポート
- もしくは freee 取り込み後に「マスタ・口座 → 取引先」画面で重複を手動統合

### Q-B5. 取引先マスタ CSV の書き出しに失敗した場合は?

書き込み失敗時 (権限不足・ディスク満杯・ファイルが開かれている等) は、以下のような整形エラーが stderr に出力されて `exit code 1` で停止します:

```
======================================================================
[エラー] 取引先マスタ CSV の書き出しに失敗しました
======================================================================
出力先: /path/to/output/freee取引先マスタ.csv
原因: PermissionError: [Errno 13] Permission denied: '/path/to/output/freee取引先マスタ.csv'

【ネクストアクション】
  1. 出力先ディレクトリの書き込み権限を確認してください
  2. ディスク残量を確認してください
  3. 他のプロセスがファイルを開いていないか確認してください
======================================================================
```

メッセージのネクストアクションに従って原因を解消し、再実行してください。

### Q-B6. interactive モードの使い方は?

`--dedup-strategy=interactive` を指定すると、同名異コードが検出されるたびに 1 件ずつ対処方法を標準入力で選べます。

```
python3 obc_to_freee.py \
  --input 上.csv 下.csv \
  --output-dir output \
  --output-prefix freee用_仕訳データ_obc変換 \
  --rows-per-file 10000 \
  --output-partners \
  --dedup-strategy=interactive
```

実行すると以下のようなプロンプトが 1 件ずつ表示されます:

```
======================================================================
同名異コードの対処
======================================================================
名前: エクシードシステム㈱

候補コード:
  (1) 742001  (借方 5 回 / 貸方 0 回、仕訳合計 5 件で使用)
  (2) 746209  (借方 12 回 / 貸方 0 回、仕訳合計 12 件で使用)

対処方法を選んでください:
  (1) コード 742001 に統合
      → 仕訳 CSV の 746209 を 742001 に書き換え
      → 取引先マスタは「エクシードシステム㈱ / 742001」1 行のみ
  (2) コード 746209 に統合
      → 仕訳 CSV の 742001 を 746209 に書き換え
      → 取引先マスタは「エクシードシステム㈱ / 746209」1 行のみ
  (3) 別名化
      → 取引先マスタは 2 行: 使用回数多い方が元名、少ない方に _2 等を付与
      → 仕訳 CSV のコードは変更しない (取引先名のみ書き換え)
  (s) スキップ (この取引先は取引先マスタから除外。手動で freee マスタ画面で対処)

選択 (1, 2, 3, s): _
```

数字または `s` を入力して Enter を押すと次の件へ。全件決まったら通常の変換処理を継続します。

**テスト・自動化での使用**: echo で自動入力できます (例: 3 件全部を 1 番で統合):

```bash
echo -e "1\n1\n1" | python3 obc_to_freee.py [引数] --dedup-strategy=interactive
```

### Q-B7. merge と suffix の使い分けは?

| 状況 | 推奨戦略 |
|------|---------|
| 旧コードと新コードが混在しており、実際には同一取引先 (名寄せ未実施) | `merge-most-used` または `interactive` で統合 |
| 同名でもコードが異なれば別取引先として freee に登録したい | `suffix` (別名化) |
| 件数が少ない・対処を手動でコントロールしたい | `interactive` |
| 全件まとめて自動処理したい | `merge-most-used` (推奨自動戦略) |

**merge の効果**: 仕訳 CSV のコードも書き換えるため、freee インポート時に「同名取引先が複数」エラーが発生しません。取引先マスタは 1 行のみになります。

**suffix の効果**: コードはそのまま、取引先名のみ `_2`, `_3` を付与して区別します。freee では別取引先として登録されます (取引先マスタに 2 行入ります)。

### Q-B8. freee で「名前が重複している取引先があります」エラーが出た場合は?

freee の取引先マスタ一括インポート時に「同名の取引先が複数あります」系のエラーが出る原因は、同名異コードの取引先が CSV に複数行含まれているためです。

**対処手順:**

1. 変換コマンドに `--dedup-strategy=merge-most-used` を追加して再実行:

   ```
   python3 obc_to_freee.py \
     --input 上.csv 下.csv \
     --output-dir output \
     --output-prefix freee用_仕訳データ_obc変換 \
     --rows-per-file 10000 \
     --output-partners \
     --dedup-strategy=merge-most-used
   ```

2. 生成された `freee取引先マスタ.csv` で同名が 1 行のみになっていることを確認

3. 仕訳 CSV も同時に再生成されるため、取引先マスタ → 仕訳 CSV の順で改めて freee にインポート

**どのコードが「正」かを自分で決めたい場合は** `--dedup-strategy=interactive` を使って 1 件ずつ選択できます。

---

## 課売返振替まわり

### Q-KR1. 課売返振替戦略はどう選べばよいですか?

監査ログに「[要確認] 課売上区分 + マイナス金額の返品疑い」が表示された場合、以下の3戦略から選んでください。

| 戦略 | 動作 | 向いているケース |
|------|------|----------------|
| `warn-only` (既定) | 何もしない。警告のみ表示 | とりあえず確認したい / 後で奉行側を直す |
| `auto-rebate` | 全件を自動で「課税売返」区分に振替 | 全件返品・値引きと確定している場合 |
| `custom` (GUI のみ) | Web 版/Python GUI 版でカードを 1 件ずつ選択 | 一部だけ振替したい場合 |

CLI での指定例:

```
python3 obc_to_freee.py [基本引数] \
  --kauuri-rebate-strategy=auto-rebate \
  --kauuri-rebate-kamoku=売上値引高
```

### Q-KR2. 「奉行原本科目を維持」を選んだのに freee 出力の科目が変わってしまいます

GUI custom モーダルで「奉行原本科目を維持」(`__original__`) を選択すると、振替後も振替前の勘定科目 (奉行に記録されていた科目) が引き継がれます。科目名ではなく税区分のみ「課税売返10%」に変更されます。

---

## 元伝票モーダルまわり

### Q-SM1. 監査ログの伝票 No をクリックすると何が表示されますか?

Web 版 / Python GUI 版の監査ログで伝票 No のリンクをクリックすると、**奉行原本の仕訳データ** (借方科目・貸方科目・金額・税区分・摘要など) をモーダルダイアログで確認できます。

課売返振替を実行した後でも、モーダルには **振替前の奉行原本データ** が表示されます (振替後のデータではありません)。これにより経理担当が奉行原本との照合を行えます。

### Q-SM2. 元伝票モーダルはどのバージョンで使えますか?

Web 版 (GitHub Pages)、Python GUI 版、JavaScript 単独版 (ダウンロード HTML) の 3 バージョンで使えます。Python CLI 版では元伝票モーダルは表示されません。

両フロントエンドで元伝票モーダルを表示します。Python GUI 版は API レスポンスの slipDetails を、JavaScript 単独版は変換時にスナップショットした originalGroups を表示します。どちらも振替前 (奉行原本) データを表示します。

